[project]
name = "api"
version = "0.1.0"
description = "API for solving linear programming problems"
requires-python = ">=3.14"
dependencies = ["fastapi[standard]>=0.136.0", "core"]

[tool.uv.sources]
core = { workspace = true }

[tool.uv]
package = true
