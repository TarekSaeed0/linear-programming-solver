[project]
name = "core"
version = "0.1.0"
description = "Core library for solving linear programming problems"
requires-python = ">=3.14"
dependencies = [
    "frozendict>=2.4.7",
    "numpy>=2.4.4",
]

[tool.uv]
package = true
